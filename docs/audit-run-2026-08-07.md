# Packaging audit run — 7 August 2026

Status: **COMPUTATIONALLY VERIFIED**.

This run was made from proof revision `de5c448` before the repository
packaging files were committed. The packaging edits did not change Python
source or tests.

## Environment

```text
Python 3.13.13 (official Windows embeddable x64 distribution)
SHA-256:
8766a8775746235e23cf5aee5027ab1060bb981d93110577adcf3508aa0cbd55
```

## Unit and regression tests

Command:

```bash
python -m unittest discover -s tests -v
```

Result:

```text
Ran 97 tests in 72.059s
OK
```

## Identity and descent verification

Command:

```bash
python scripts/verify_claims.py --limit 100000
```

Output:

```text
verified m-space normal form for 1 <= m <= 100,000
verified B(q) < q for 1 <= q <= 100,000
verified descent blocks for odd 3 <= n <= 200,001
nonterminal descent cases: 99,983
D(n)=1 cases: 17
```

These are finite regression checks. The infinite claims depend on the
symbolic proofs in the documentation, not on this cutoff.

## One-command audit after certificate integration

Status: **COMPUTATIONALLY VERIFIED**.

Command:

```bash
python audit.py
```

Key output:

```text
checked 60 local Markdown links
Lean sources contain no axiom/admit/sorry placeholders
Ran 101 tests in 75.883s
OK
verified m-space normal form for 1 <= m <= 100,000
verified B(q) < q for 1 <= q <= 100,000
verified descent blocks for odd 3 <= n <= 200,001
generated nodes: 59
certificate SHA-256:
98a73feeeb705e5999f8853f106eb753c17e557aad794317178a56a91d5111ed
VALID finite outcome certificate: q=100 is WIN; 59 nodes; max rank 44448
=== AUDIT PASSED ===
All machine-checkable local stages passed in 79.84s.
```

The digest identifies the deterministic certificate emitted by Python 3.13.13
with the displayed parameters. The final success line does not expand the
scope of the finite checks to the global theorem.

## Optional Lean foundation

Status: **COMPUTATIONALLY VERIFIED BUILD**.

```text
Lean 4.32.1, commit f054605aea4b840552cca2e725580bffd1e1b704
lake build
Build completed successfully (4 jobs).
```

The project has no third-party Lean dependencies. The exact formalized subset
is listed in `../formal/COVERAGE.md`; the global no-`DRAW` theorem is not yet
Lean-checked.
