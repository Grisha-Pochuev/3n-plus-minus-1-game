# Proof ledger

Update this table whenever the status of a substantial claim changes.

| Claim | Status | Location / evidence |
|---|---|---|
| Original game is exactly represented by `F(m), F(R(m))` in `m=(n-1)/2` coordinates | **PROVED** | `docs/normal-form.md`; finite regression tests in `tests/test_game.py` |
| Conjugated game is `A(q)=F(q)`, `B(q)=R(F(q))` | **PROVED** | `docs/normal-form.md` |
| `B(q)<q` for every `q>0` | **PROVED** | `docs/normal-form.md` |
| `D(D(n))<n` unless `D(n)=1` | **PROVED** | `docs/verified-results.md` |
| `D(U(D(n)))<n` unless the game already ended | **PROVED** | `docs/verified-results.md` |
| One chosen player always choosing `D` forces finite termination | **DISPROVED** | `5 -> 7 -> 5` with the opponent choosing `U`; `docs/verified-results.md`, Section 3 |
| Always choosing `D` forces a win | **FALSE** | The same `5 -> 7 -> 5` response already prevents termination |
| Minimal draw implies the entire chain `d,A(d),A^2(d),...` is drawn | **DISPROVED AS AN ARGUMENT** | `docs/pitfalls.md` |
| A fixed small horizon of side branches always contains a losing node | **DISPROVED for several proposed small constants** | inherited exploratory counterexamples; see `docs/unverified-leads.md` |
| A fixed modulus `2^K` determines `R` | **FALSE** | `docs/normal-form.md` |
| In Gray coordinates, `R` deletes the trailing ones and the preceding zero via `G(R(x))=G(x)>>(v2(G(x)+1)+1)` | **PROVED** | `docs/normal-form.md`; `alternating_suffix_remainder_via_gray` |
| The Gray-coordinate expanding move `Gamma` is recognized exactly by an eight-state LSB-first transducer | **PROVED** | `docs/normal-form.md`; `src/optimal_3n1/transducer.py`; exhaustive regression in `tests/test_game.py` |
| Bounded retrograde labels and extracted proof DAGs are sound | **PROVED FOR THE IMPLEMENTATION LOGIC** | `src/optimal_3n1/retrograde.py`; `scripts/extract_proof.py`; `tests/test_retrograde.py` |
| Outside `q=1,3,12,14 mod 16`, consecutive side branches satisfy `B(A(q))=A(B(q))` or `B(A(q))=B(B(q))` | **PROVED** | `docs/verified-results.md`, Section 5; `side_branch_relation` |
| All four exceptional side branches have the explicit affine/reduced formulas in Section 5 | **PROVED** | `docs/verified-results.md`, Section 5; `exceptional_side_branch_values` |
| Every long exceptional suffix has the uniform `(r,k,e)` formula of Section 5 | **PROVED** | `docs/verified-results.md`, Section 5; `long_side_branch_value` |
| Four consecutive positions on one `A`-ray cannot all be `WIN` | **PROVED** | `docs/verified-results.md`, Section 5 |
| All predecessors through `B` are parameterized by attached alternating words | **PROVED** | `docs/verified-results.md`, Section 6; `transformed_B_predecessors` |
| Every length-three word with one `A` and two `B` moves is smaller than its input | **PROVED** | `docs/verified-results.md`, Section 7; `transformed_BBA`, `transformed_BAB`, `transformed_ABB` |
| A nonexceptional three-vertex `WIN` path has endpoint `B(A(q))` and decreases finite proof height by at least two | **PROVED** | `docs/verified-results.md`, Section 9; finite regression in `tests/test_retrograde.py` |
| Every infinite path consisting only of `WIN` positions visits the exceptional classes infinitely often | **PROVED** | `docs/verified-results.md`, Section 9 |
| Every hypothetical DRAW set has a canonical base-draw skeleton; skeleton steps with two or more `B` moves strictly decrease | **PROVED** | `docs/verified-results.md`, Section 10 |
| A smallest DRAW has the exact local `BB`/`BAB` outcome fingerprint of Section 10 | **PROVED** | `docs/verified-results.md`, Section 10 |
| Away from exceptional classes, canonical base-DRAW transitions obey the size-change table of Section 11 | **PROVED** | `docs/verified-results.md`, Section 11 |
| A minimum-proof-height DRAW-to-WIN boundary edge must touch an exceptional residue class | **PROVED** | `docs/verified-results.md`, Section 12 |
| Height-one WIN positions are exactly the positive `s` with `B(s)=0`, and all are exceptional | **PROVED** | `docs/verified-results.md`, Section 13 |
| If `A(q)` is a height-one WIN, then `B(q)` is also a height-one WIN and `q` is LOSS | **PROVED** | `docs/verified-results.md`, Section 13 |
| For odd `a` and `r>=3`, the children of `X_r(a)=a*2^r-1` are `X_(r-1)(3a)` and `X_(r-2)(3a)` | **PROVED** | `docs/verified-results.md`, Section 14; `dyadic_minus_one_children` |
| The boundary states `X_1(a)` and `X_2(a)` have the same `B`-child | **PROVED** | `docs/verified-results.md`, Section 14; regression in `tests/test_game.py` |
| Both constant-tail families `Q_r^epsilon(a)=a*2^r-epsilon` obey the same closed recurrence for `r>=3` | **PROVED** | `docs/verified-results.md`, Section 14; `constant_tail_children` |
| At `r=1`, the expanding move flips the tail bit and uses the exact signed coefficient map `oddpart(3a+1-2epsilon)` | **PROVED** | `docs/verified-results.md`, Section 14; regression in `tests/test_game.py` |
| The common `B`-child of `Q_1^epsilon(a),Q_2^epsilon(a)` has canonical coefficient at most `(3a+1)/4`, with one resolved equality case | **PROVED** | `docs/verified-results.md`, Section 15; `constant_tail_coordinates`; regression in `tests/test_game.py` |
| A minimum-coefficient DRAW at constant-tail exponent one must take the valuation-one signed transition | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 15 |
| The second signed transition of a minimum-coefficient exponent-one DRAW has the `v=2`, adjacent-pair `v=3`, or impossible `v>=4` trichotomy | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 16; arithmetic regression in `tests/test_game.py` |
| A DRAW with coefficient divisible by three and exponent at least two forces a DRAW with coefficient divided by three | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 16; frame identities in `tests/test_game.py` |
| Every odd coefficient is uniquely `3^k J(s)`, and at `k=0,r=1` the signed transition selects exactly `A(s)` or `B(s)` | **PROVED** | `docs/verified-results.md`, Section 17; `constant_tail_coefficient_source`; `source_boundary_transition` |
| A minimum-source DRAW at `k=0,r=1` must use the phase selecting `A(s)` | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 17 |
| For odd `w`, the canonical coefficients of `3w` and `3w-1` are `J(R(w))` and `J(R(2w-1))` | **PROVED** | `docs/verified-results.md`, Section 18; regression in `tests/test_game.py` |
| Two exponent-one source lifts return to source `B(A(s))` outside the four exceptional classes; an exceptional return has strictly smaller source | **PROVED** | `docs/verified-results.md`, Section 18; `constant_tail_source_coordinates`; regression in `tests/test_game.py` |
| A minimum-source lifted DRAW can escape its second side child only through a nondecreasing ordinary source return `B(A(s))>=s` | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 18 |
| The ordinary side return is nondecreasing exactly for `s=0,5,10,15 mod 16`, with the four affine formulas of Section 19 | **PROVED** | `docs/verified-results.md`, Section 19; regression in `tests/test_game.py` |
| In the four phase-mismatch subclasses modulo 32, a minimum-source lifted DRAW forces the exact `LOSS/WIN/DRAW` adjacent-pair fingerprint of Section 19 | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 19 |
| The ensuing factor-three DRAW has valuation-one expanding child with exact source `3A(B(A(s)))+1` | **PROVED ARITHMETIC IDENTITY; DRAW CONTINUATION OPEN** | `docs/verified-results.md`, Section 19; regression in `tests/test_game.py` |
| The competing child of that factor-three DRAW closes a larger diamond and contradicts the forced LOSS sibling | **PROVED** | `docs/verified-results.md`, Section 20; regression in `tests/test_game.py` |
| A globally minimum-source DRAW lift can make a nondecreasing side return only in the phase-match classes `0,10,21,31 mod 32` | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Sections 18--20 |
| In every phase-match return, the common child's coefficient source is below `s`, while the factor-three state's children are adjacent lifts with source equal to that common child | **PROVED** | `docs/verified-results.md`, Section 21; regression in `tests/test_game.py` |
| A long returned suffix occurs exactly for `s=21,63,64,106 mod 128`, and all four classes contradict the minimum-source DRAW hypothesis | **PROVED CONDITIONAL EXCLUSION** | `docs/verified-results.md`, Section 21; regression in `tests/test_game.py` |
| Every phase-match returned suffix of length at least three creates another DRAW/WIN boundary whose WIN proof height drops by at least two | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 22; regression in `tests/test_game.py` |
| An indefinitely surviving phase-match boundary reduction must eventually enter one of eight suffix-length-two classes modulo 128 | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Sections 20--22 |
| The eight suffix-length-two classes reduce to the exact lifted WIN-source / adjacent LOSS-source fork of Section 23 | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 23; regression in `tests/test_game.py` |
| In that fork the ordinary returned source `c` has a forced WIN child `r` and forced LOSS child `ell`, with the two lifted phases selecting them respectively | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 23 |
| The side child of every `A`-selecting source lift is an ordinary child of the selected source `A(x)` | **PROVED** | `docs/verified-results.md`, Section 24; regression in `tests/test_game.py` |
| In the `31,53,74,96 mod 128` LOSS-source fork, the adjacent DRAW pair creates a boundary whose WIN height drops by at least two | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 24 |
| The factor-three escape from every `B`-selecting source lift closes the universal large diamond of Section 25 | **PROVED** | `docs/verified-results.md`, Section 25; regression in `tests/test_game.py` |
| A DRAW in a `B`-selecting boundary lift, or in a parent of its exponent-one/two pair, transfers to an adjacent DRAW frame over the ordinary source `B(x)` | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 25 |
| In the final eight length-two rows, the transferred source `B(c)` is nondecreasing exactly in eight classes modulo 256, equivalently exactly when the signed valuation is two | **PROVED** | `docs/verified-results.md`, Section 26; regression in `tests/test_game.py` |
| Under the minimum-source hypothesis, every surviving B-transfer is an exponent-one/two pair whose next phase is fixed by eight classes modulo 512 | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 26 |
| A valuation-two B-selection preserves the exact exponent-one/two DRAW obligation at the ordinary source `B(x)` | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 27 |
| After the final length-two fork, at most two additional B-source transfers can survive before either dropping below the minimum DRAW source or reaching the A-selecting phase | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 27; regression in `tests/test_game.py` |
| At the final A-selecting obligation, the common side child's own coefficient source is below `s`, forcing the exact canonical-lift/factor-frame outcome fork of Section 28 | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 28 |
| A surviving factor-three escape from that A-selecting fork is an adjacent frame over its WIN side child with lower exponent at most three | **PROVED CONDITIONAL REDUCTION** | `docs/verified-results.md`, Section 28; regression in `tests/test_game.py` |
| In the final factor fork, the lower factor child and the expanding child of the retained LOSS state are exact opposite-tail twins at the same source and exponent | **PROVED** | `docs/verified-results.md`, Section 29; regression in `tests/test_game.py` |
| The two members of the final factor frame have the exact common child of Section 30; at exponents two and three its opposite-tail twin is the expanding child of the retained WIN state | **PROVED** | Section 30 of docs/verified-results.md; regression in tests/test_game.py |
| If that common factor child is DRAW, the next twin level contains a forced opposite-tail LOSS (or the lower opposite-tail LOSS alternative at exponent three) | **PROVED CONDITIONAL REDUCTION** | Section 30 of docs/verified-results.md |
| At lower factor exponent one, the factor source is exactly A^2(x); an A-selecting common DRAW child has source below x, while a source-surviving B-selection has valuation only two or three and the explicit bounds of Section 31 | **PROVED CONDITIONAL REDUCTION** | Section 31 of docs/verified-results.md; regression in tests/test_game.py |
| After a surviving exponent-two factor switch, the next signed valuation is at most four; after exponent three, the two possible signed continuations have valuations at most three and four | **PROVED CONDITIONAL REDUCTION** | Section 32 of docs/verified-results.md; regression in tests/test_game.py |
| Every exponent-three factor continuation with first signed valuation at least four returns to a B-selecting DRAW lift at a source below 16s, with both retained side states below the minimum source | **PROVED CONDITIONAL REDUCTION** | Section 33 of docs/verified-results.md; regression in tests/test_game.py |
| In the high-valuation exponent-three return, the two retained WIN states force an ordinary WIN W=B(A(u)) whose canonical proof height is at most h(C)-2 | **PROVED CONDITIONAL REDUCTION** | Section 34 of docs/verified-results.md; regression in tests/test_game.py |
| The high-valuation B-selecting lift transfers with valuation two to an obligation over Z=B(u), whose selected ordinary child is the lower-height WIN W=B(A(u)) | **PROVED CONDITIONAL REDUCTION** | Section 35 of docs/verified-results.md; regression in tests/test_game.py |
| Every exponent-three continuation with first signed valuation at least six creates a new DRAW/WIN boundary whose WIN height drops by at least two | **PROVED CONDITIONAL REDUCTION** | Section 35 of docs/verified-results.md; regression in tests/test_game.py |
| In the remaining v=4 exponent-three branch, every source-surviving transfer has valuation at most five and lower frame exponent at most four | **PROVED CONDITIONAL REDUCTION** | Section 36 of docs/verified-results.md; regression in tests/test_game.py |
| In the remaining v=5 exponent-three branch, the common side state is an exact lift over the forced ordinary LOSS source M and a surviving DRAW has exponent at most three | **PROVED CONDITIONAL REDUCTION** | Section 36 of docs/verified-results.md; regression in tests/test_game.py |
| In the v=4 branch with valuation three, four, or five, the retained ordinary LOSS state has the exact lower WIN ladder of Section 37 beneath the upper adjacent DRAW frame | **PROVED CONDITIONAL REDUCTION** | Section 37 of docs/verified-results.md; regression in tests/test_game.py |
| In the v=5 branch, a DRAW common side state can occur only in the exceptional rows Z=3 or 12 mod 16, and then its exact exponent over the forced LOSS source M is two or three | **PROVED CONDITIONAL REDUCTION** | Section 38 of docs/verified-results.md; regression in tests/test_game.py |
| Every surviving DRAW common side state in the v=5 branch creates a new boundary whose WIN endpoint has height at most h(W)-2 | **PROVED CONDITIONAL REDUCTION** | Section 39 of docs/verified-results.md; regression in tests/test_game.py |
| Each surviving v=4 ladder with j=3,4,5 forces a LOSS witness in the fixed set C,D,T1,T2 according to the finite outcome split of Section 40 | **PROVED CONDITIONAL REDUCTION** | Section 40 of docs/verified-results.md; arithmetic regression in tests/test_game.py |
| A one-sided LOSS witness shared with a DRAW branch either exposes a boundary WIN, strictly lowers boundary height by two, or has an exceptional WIN parent; in the v=4 ladders only S1=1,14 and S2=3,12 mod 16 remain exceptional | **PROVED CONDITIONAL REDUCTION** | Section 41 of docs/verified-results.md; arithmetic regression in tests/test_game.py |
| The exceptional S1 row in the v=4 ladders drops below the minimum DRAW source; an exceptional S2 continuation is an exact lift over its ordinary LOSS source C with exponent at most four for j=3 and at most three for j=4 | **PROVED CONDITIONAL REDUCTION** | Section 42 of docs/verified-results.md; regression in tests/test_game.py |
| In the v=4 j=2 row, Z is nonexceptional, the phase at W selects the explicit WIN source B(M), W<10s, and after at most eight valuation-two B transfers the obligation reaches the A-phase, drops below s, or enters an exponent-two/three frame | **PROVED CONDITIONAL REDUCTION** | Section 43 of docs/verified-results.md; regression in tests/test_game.py |
| The A-selecting half of the v=4 j=2 return either exposes a boundary WIN, strictly lowers boundary height by two, or has an exceptional selected WIN source R | **PROVED CONDITIONAL REDUCTION** | Section 44 of docs/verified-results.md; regression in tests/test_game.py |
| In the r=2 switch with signed valuation three or four, every nonexceptional E produces exactly the known j=3 or j=4 finite ladder and hence a boundary WIN, strict height descent, or one of the explicit S1/S2 exceptional rows; only E=3,12 mod 16 adds an orientation exception | **PROVED CONDITIONAL REDUCTION** | Section 45 of docs/verified-results.md; regression in tests/test_game.py |
| The exceptional E=3,12 orientation in the r=2 switch is an exact lift over its explicit LOSS source C<81s/16, and a surviving DRAW lift has exponent at most three | **PROVED CONDITIONAL REDUCTION** | Section 46 of docs/verified-results.md; regression in tests/test_game.py |
| In the r=2 switch, valuation two returns to an exact obligation O(t,e) with t<81s/16; valuation one returns either to O(t,e) with t<81s/8 or to a raw-side DRAW whose coefficient source is below (243s+4)/96 | **PROVED CONDITIONAL REDUCTION** | Section 47 of docs/verified-results.md; regression in tests/test_game.py |
| Every r=1 common-DRAW continuation is one of three bounded types: strict source return, O(y,e) with y<27s/8, or Q2 over y<27s/16 whose side source is below (81s+16)/64 or whose factor sibling is DRAW | **PROVED CONDITIONAL REDUCTION** | Section 48 of docs/verified-results.md; regression in tests/test_game.py |
| In the low r=3 contracting-DRAW branch, valuation two returns to O(t,e) below (243s+44)/96, while valuations one and three return a raw-side or Q2 DRAW whose coefficient source is below (243s+44)/192 | **PROVED CONDITIONAL REDUCTION** | Section 49 of docs/verified-results.md; regression in tests/test_game.py |
| In the low r=3 expanding-DRAW branch, valuations two through four are adjacent frames with bounded sources, while valuation one returns to O(u,e) or a raw-side DRAW below (729s+116)/192 | **PROVED CONDITIONAL REDUCTION** | Section 50 of docs/verified-results.md; regression in tests/test_game.py |
| If the WIN endpoint P in the factor alternative has globally minimum boundary height, both children of its unique LOSS witness V have height at most h(P)-2 and no DRAW parent | **PROVED CONDITIONAL REDUCTION** | Section 51 of docs/verified-results.md |
| The A-selecting obligation source x is finite; at factor exponent r=2,3 one has b=B(A(x)), x is nonexceptional, and x WIN implies the boundary endpoint b satisfies h(b)<=h(x)-2 | **PROVED CONDITIONAL REDUCTION** | Section 52 of docs/verified-results.md; regression in tests/test_game.py |
| In the exponent-one factor switch, an A-selecting continuation forces a boundary-height drop; every DRAW continuation selects a LOSS source and retains c=B(A(x)) with the same outcome as x and h(c)<=h(x)-2, with only one explicit exceptional lift form | **PROVED CONDITIONAL REDUCTION** | Section 53 of docs/verified-results.md; regression in tests/test_game.py |
| For 9J(b)+1-2g=2^vJ(t), the returned source is a lift over B(b) at v=1, A^2(b) at v=2, and B(A(b)) at v>=3; the last row lowers height by two when b is WIN and nonexceptional | **PROVED CONDITIONAL REDUCTION** | Section 54 of docs/verified-results.md; regression in tests/test_game.py |
| In the valuation-two factor return, t=A^2(b) is finite in the r=2 and contracting r=3 branches; its returned phase is A exactly for exceptional b, and otherwise it is B with a retained lower-height WIN q=B(A(b)) | **PROVED CONDITIONAL REDUCTION** | Section 55 of docs/verified-results.md; regression in tests/test_game.py |
| One B-transfer after a nonexceptional valuation-two return either yields an exact lift over the lower-height WIN q=B(A(b)), or the fixed exponent-three/two frame over A(q); all higher valuations fall below the minimum source | **PROVED CONDITIONAL REDUCTION** | Section 56 of docs/verified-results.md; regression in tests/test_game.py |
| For a WIN q and a canonical LOSS child ell, the side return B(A(q)) is either a WIN child of ell with height at most h(q)-2, or, only in the exceptional witness orientation, an exact factor-free lift over ell with h(ell)=h(q)-1 | **PROVED CONDITIONAL RANK TRANSPORT** | Section 57 of docs/verified-results.md; regression in tests/test_game.py |
| The residual exponent-three/two frame transfers either to the factor-one exponent-two/one frame, or only in the B-selecting phase to an exact lift over the typed token B(A(q)); an A-selecting side exit has source below the global minimum | **PROVED CONDITIONAL REDUCTION** | Section 58 of docs/verified-results.md; regression in tests/test_game.py |
| The factor-one exponent-two/one escape reaches a signed or side boundary after at most one further factor level; its 9J(x) and 27J(x) valuations are at most four and five respectively whenever their factor-free source survives the global minimum | **PROVED CONDITIONAL REDUCTION** | Section 59 of docs/verified-results.md; regression in tests/test_game.py |
| The 9J(x) and 27J(x) factor exits are coupled: valuation at least two in the first makes the second a valuation-one exact lift over the first source, while valuation one first lifts over B(x) and makes the second one ordinary selected-source step from that lift | **PROVED CONDITIONAL RANK TRANSPORT** | Section 60 of docs/verified-results.md; regression in tests/test_game.py |
| When the finite factor source x is LOSS, every 9J(x)/27J(x) exit retains a lower-height ordinary WIN token A(x) or B(x), refined after valuation at least two to the canonical side-return token below A(x); no returned source is unmarked | **PROVED CONDITIONAL RANK TRANSPORT** | Section 61 of docs/verified-results.md |
| When the residual factor source x=A(q) is WIN, every 9J(x) return except valuation two is either an exact lift over p=B(x) or an ordinary child of p; valuation two is the already normalized recursive three/two-frame type | **PROVED CONDITIONAL RANK TRANSPORT** | Section 62 of docs/verified-results.md; regression in tests/test_game.py |
| If a nonexceptional WIN p has a WIN child q', then the next A-source A(q') is either a WIN of height at most h(p)-2 or a LOSS; hence an all-ordinary valuation-two recycle enters a strict WIN descent or the LOSS-source rank type | **PROVED CONDITIONAL RANK TRANSPORT** | Section 63 of docs/verified-results.md; arithmetic regression in tests/test_game.py |
| In every exceptional-token valuation-two row, the suffix at A(x) has length exactly two and the next recursive source A(B(A(x))) is strictly smaller than x; the sixteen complete rows are fixed modulo 256 | **PROVED CONDITIONAL REDUCTION** | Section 64 of docs/verified-results.md; regression in tests/test_game.py |
| A consecutive A-selecting lift streak exposes within four side tests either the known B-phase, a boundary WIN two height levels below the preceding endpoint, or an exceptional exact lift over a height-lower LOSS source that is also numerically below the streak's initial source | **PROVED CONDITIONAL RANK TRANSPORT** | Section 65 of docs/verified-results.md; regression in tests/test_game.py |
| After a canonical A-streak, at most eight exact valuation-two B-transfers can survive; every earlier source-surviving higher-valuation exit has valuation only three or four | **PROVED CONDITIONAL REDUCTION** | Section 66 of docs/verified-results.md; regression in tests/test_game.py |
| A fixed horizon for the total phase-only A/B alternation is impossible: for every k there is an exact three-free source with k consecutive A,B-valuation-two pairs | **DISPROVED AS A RANK STRATEGY** | Section 67 of docs/verified-results.md; regression in tests/test_game.py |
| In the exceptional canonical side return, the two children of the DRAW state are adjacent factor-free lifts over the unique LOSS child of the preceding boundary WIN; hence one of those lifts is an actual DRAW with a source one proof level lower | **PROVED BOUNDARY EXPOSURE** | Section 68 of docs/verified-results.md; regression in tests/test_game.py |
| A minimum height-one boundary by itself forces A(D), not A(B(D)), to be DRAW; using Section 68 on its WIN endpoint without an additional identity is invalid | **DISPROVED AS AN ARGUMENT** | docs/pitfalls.md, Section 14 |
| Every height-one boundary has a three-type continuation: an ordinary zero-source DRAW frame, an ordinary new boundary with endpoint A(s), or an exceptional adjacent DRAW frame over the height-one WIN source s | **PROVED CONDITIONAL NORMAL FORM** | Section 69 of docs/verified-results.md; regression in tests/test_game.py |
| If B(s)=0, the two children of A(s) are an adjacent factor-free zero-source frame; when A(s) is WIN that frame contains its LOSS witness, and when A(s) is DRAW it contains a DRAW | **PROVED** | Section 70 of docs/verified-results.md; regression in tests/test_game.py |
| Every zero-source DRAW frame reaches an exponent-one/two factor boundary whose DRAW lies on the raw side R(3^n-delta) or the signed 3^n+1-2delta exit; the signed valuation is 1 or 2 except for v2(3^n-1)=2+v2(n) at even n | **PROVED CONDITIONAL NORMAL FORM** | Section 71 of docs/verified-results.md; regression in tests/test_game.py |
| The raw and signed zero-source exits share one ordinary source: at valuation at least two they are an adjacent factor-free frame, while at valuation one the raw exit is A(y) or B(y) and either the signed lift is DRAW or the raw DRAW has strictly smaller coefficient source | **PROVED CONDITIONAL NORMAL FORM** | Section 72 of docs/verified-results.md; regression in tests/test_game.py |
| A zero-source LOSS token crosses every two long-tail levels as another actual LOSS with proof height lower by at least two; at exponent one/two/three it reaches the same raw/signed boundary, and the sole unbounded even-minus frame retains a strictly lower-height LOSS member | **PROVED CONDITIONAL RANK TRANSPORT** | Section 73 of docs/verified-results.md; regression in tests/test_game.py |
| If a DRAW-to-WIN boundary endpoint has an adjacent child frame containing LOSS, two consecutive nonexceptional DRAW-spine states expose the common WIN child as a new boundary endpoint at least two proof levels lower | **PROVED CONDITIONAL BOUNDARY DESCENT** | Section 74 of docs/verified-results.md; regression in tests/test_game.py |
| In the height-one ordinary successor, an alternating predecessor suffix of length at least five forces both consecutive DRAW-spine states outside the exceptional classes; hence only suffix lengths one through four can avoid the Section 74 height descent | **PROVED CONDITIONAL FINITE REDUCTION** | Section 75 of docs/verified-results.md; regression in tests/test_game.py |
| For a nonexceptional height-one boundary parent, the outcome-compatible successor has exactly four short residue rows, and both consecutive DRAW-spine states are nonexceptional in every row; hence the adjacent LOSS token always yields a boundary-height drop | **PROVED CONDITIONAL BOUNDARY DESCENT** | Section 76 of docs/verified-results.md; regression in tests/test_game.py |
| Every height-one source s has 3J(s)=2^(L+2)+1-2delta: its A-selecting phase delta has valuation one and returns A(s), while its opposite phase has valuation L+2 and returns the terminal source zero | **PROVED** | Section 77 of docs/verified-results.md; regression in tests/test_game.py |
| After k>=1 accumulated factor levels over a height-one source, the A-phase valuation is 2 for odd k and 1 for even k; the B-phase valuation is the smaller of L+2 and v2(3^k-1), except for the sole resonance v2(k)=L where it exceeds L+2 | **PROVED** | Section 78 of docs/verified-results.md; regression in tests/test_game.py |
| At every pair of consecutive factor levels, valuation at least two is followed by valuation one and an exact lift over the current source; valuation one is followed by one ordinary selected-source step, with the terminal source-zero case factored directly | **PROVED UNIVERSAL COUPLING** | Section 79 of docs/verified-results.md; regression in tests/test_game.py |
| Every adjacent exponent-two/one frame containing DRAW has a DRAW among the raw and signed exits of its current or immediately following factor level; a marked scan cannot skip both coupled levels | **PROVED UNIVERSAL OUTCOME NORMAL FORM** | Section 80 of docs/verified-results.md; regression in tests/test_game.py |
| At a factor boundary, valuation at least two makes the raw and signed exits an adjacent frame over one source; valuation one makes every positive raw DRAW have coefficient source strictly below the signed returned source | **PROVED UNIVERSAL RANK NORMAL FORM** | Section 81 of docs/verified-results.md; regression in tests/test_game.py |
| The valuation-one successor of a valuation-v factor exit has raw child A(u) for v=2 and B(u) for v>=3; for v>=4 that raw child has exact coordinates (t,1,v-3,1-e), preserving the returned source with one factor of three | **PROVED UNIVERSAL RETURN IDENTITY** | Section 82 of docs/verified-results.md; regression in tests/test_game.py |
| The two short high-to-one raw returns are exactly Q_0^g(3J(t)) at v=2 and R(Q_1^g(3J(t))) at v=3; both remain at the factor-one boundary over t before the sole explicit suffix deletion | **PROVED UNIVERSAL SHORT-RETURN NORMAL FORM** | Section 83 of docs/verified-results.md; regression in tests/test_game.py |
| Every four-exit factor scan with signed valuation v>=5 yields an adjacent DRAW frame either over its returned source t or over the explicit B-child Q_(v-3)^g(3J(t)); every height-one resonant row satisfies v>=5 | **PROVED HIGH-VALUATION OUTCOME NORMAL FORM** | Section 84 of docs/verified-results.md; regression in tests/test_game.py |
| The v=2 raw successor is an exact factor-free lift over the ordinary phase-selected child A(t) or B(t), while the v=3 raw successor has strictly smaller coefficient source than t whenever positive | **PROVED UNIVERSAL SMALL-VALUATION RETURN** | Section 85 of docs/verified-results.md; regression in tests/test_game.py |
| In the second v>=5 frame, a DRAW source c returns immediately to a frame over t; otherwise c is a finite WIN/LOSS token, ordinary for v>=6, with only one explicit v=5 exceptional lift geometry | **PROVED HIGH-FRAME TOKEN NORMAL FORM** | Section 86 of docs/verified-results.md; regression in tests/test_game.py |
| Any DRAW exponent-two/one frame with coefficient divisible by 3 pulls back to a factor-free DRAW lift unless its upper member is LOSS and lower member DRAW; that sole gate exposes a boundary WIN two proof levels below the exact reverse parent | **PROVED UNIVERSAL REVERSE OUTCOME NORMAL FORM** | Section 87 of docs/verified-results.md; regression in tests/test_game.py |
| When the upper-LOSS/lower-DRAW gate occurs at factor level k>=2 on a DRAW spine, its exact reverse WIN parent is itself a boundary endpoint and the common forward WIN child is a second boundary endpoint at least two proof levels lower | **PROVED INTERNAL FACTOR HEIGHT DESCENT** | Section 88 of docs/verified-results.md; regression in tests/test_game.py |
| Every typed exponent-three/two DRAW frame that reaches the first upper-LOSS/lower-DRAW factor gate has an ordinary inverse-A DRAW parent, so the same two-level boundary-height descent also holds at factor level one | **PROVED FIRST-FACTOR HEIGHT DESCENT** | Section 89 of docs/verified-results.md; regression in tests/test_game.py |
| The second frame in every valuation-v>=5 return is generated by a valuation-two transition whose output phase is A-selecting at its finite source c; whenever this branch carries DRAW it is exactly the obligation O(c,alpha(c)) | **PROVED HIGH-RETURN OBLIGATION CLOSURE** | Section 90 of docs/verified-results.md; regression in tests/test_game.py |
| Every A-selecting obligation, without a source-window assumption, either exposes a DRAW with strictly smaller coefficient source, continues canonically to A(x), or produces an adjacent DRAW frame over its common WIN child while retaining the exact LOSS sibling | **PROVED UNIVERSAL A-OBLIGATION FORK** | Section 91 of docs/verified-results.md; regression in tests/test_game.py |
| In the factor row of an A-selecting obligation, both ordinary children of the WIN frame source b have coefficient source strictly below the generating source x | **PROVED FACTOR-TOKEN SOURCE DESCENT** | Section 92 of docs/verified-results.md; regression in tests/test_game.py |
| Every canonical A-continuation from an arbitrary A-selecting obligation reaches within four side tests either a B-selecting phase, a boundary endpoint two proof levels lower, or an adjacent DRAW frame over a lower-height LOSS source strictly below the initial source | **PROVED UNIVERSAL CANONICAL-STREAK EXIT** | Section 93 of docs/verified-results.md; regression in tests/test_game.py |
| In the exponent-one factor fork, the retained LOSS forces the phase-selected child of b to be LOSS; the fork then either lowers the boundary endpoint height by two or transfers DRAW to a frame over that actual LOSS token with coefficient source below x | **PROVED EXPONENT-ONE TOKEN CLOSURE** | Section 94 of docs/verified-results.md; regression in tests/test_game.py |
| In every factor fork with lower exponent at least two, the generating source x is nonexceptional and b is a common child of A(x),B(x); according to the outcome of x, the fork exposes a smaller-source DRAW, a boundary WIN two proof levels below x, or the LOSS token A^2(x) two levels below x | **PROVED HIGHER-FACTOR RANK FORK** | Section 95 of docs/verified-results.md; regression in tests/test_game.py |
| Every canonical A-to-B switch either returns a factor-free DRAW frame at a strictly smaller source (valuation at least three) or, at valuation two, exposes a smaller-source DRAW or a WIN/LOSS token at least two proof levels lower; repeated A,B2 pairs therefore strictly lower WIN-source height | **PROVED PHASE-ALTERNATION RANK** | Section 96 of docs/verified-results.md; regression in tests/test_game.py |
| In the A-selecting obligation created by a high return, the common side is the exponent-zero state 27J(t)-g at v=5, one explicit signed-boundary remainder at v=6, and Q_(v-6)^g(27J(t)) with unchanged coefficient source t for every v>=7 | **PROVED HIGH-RETURN ANCHOR CLOSURE** | Section 97 of docs/verified-results.md; regression in tests/test_game.py |
| If the common side of a v=5 or v=6 high-return obligation is DRAW, the finite return token cannot be discarded: the row yields a LOSS token or boundary WIN at least two proof levels lower, or the exact exceptional adjacent frame over the height-lower LOSS child | **PROVED SHORT HIGH-RETURN TOKEN CLOSURE** | Section 98 of docs/verified-results.md; regression in tests/test_game.py |
| If an A-selecting obligation has a known finite source and its common side is WIN, it exports before branching either a LOSS/WIN token at least two proof levels lower or the exact exceptional adjacent pair over its canonical height-lower LOSS child | **PROVED FINITE-SOURCE WIN-SIDE TRANSPORT** | Section 99 of docs/verified-results.md; regression in tests/test_game.py |
| In the second high-return alternative, the predecessor u and returned source c=B(u) are both WIN, A(u) is LOSS, and the common child B(A(u)) is WIN at least two proof levels below u; this child has not yet been exposed inside the new obligation | **PROVED HIGH-RETURN PREDECESSOR DIAMOND** | Section 100 of docs/verified-results.md; regression in tests/test_game.py |
| The common side b of a high-return obligation is A(p) at v=5 and B(p) at v>=6 for the lower predecessor token p=A(c); an ordinary DRAW b exposes a boundary endpoint at least four levels below u, a canonical continuation has source p itself, and the remaining factor fork has an explicit lower-height WIN child | **PROVED HIGH-RETURN COMMON-SIDE ATTACHMENT** | Section 101 of docs/verified-results.md; regression in tests/test_game.py |
| In a high-return factor fork the transition at p has valuation 1, at least 3, or 2 according as v=5, v=6, or v>=7; for v>=7 the factor frame is always exponent three/two and its next phase selects the marked WIN child's actual LOSS sibling, with a high valuation possible only at v=8 | **PROVED MARKED HIGH-RETURN FACTOR TABLE** | Section 102 of docs/verified-results.md; regression in tests/test_game.py |
| For every v>=7 high-return factor fork, the two-token height falls componentwise from (u,c) to (p,b) and then to (q,y), with h(q)+h(y)<=h(u)+h(c)-7; the same holds at v=6 when p is ordinary | **PROVED TWO-TOKEN HIGH-RETURN DESCENT** | Section 103 of docs/verified-results.md; arithmetic regressions in tests/test_game.py |
| For v>=9, the marked WIN/LOSS children q,y of the high-return factor source have a common WIN child r with h(r)<=h(y)-1; the first ninefold raw exit is a lift over r, with exponent at least two at v=9 and exactly one for v>=10 | **PROVED FIRST RAW PAIRED-TOKEN DESCENT** | Section 104 of docs/verified-results.md; regression in tests/test_game.py |
| For v>=10, the first ninefold signed exit is an A-selecting lift whose raw selected source is the lift over r from Section 104; its common side is exactly the signed transition from r, with valuation 1, at least 3, or 2 at v=10, v=11, or v>=12 | **PROVED FIRST SIGNED LOWER-TOKEN RE-ENTRY** | Section 105 of docs/verified-results.md; regression in tests/test_game.py |
| For v>=10, if neither first factor exit is DRAW, the second factor numerator is exactly 4J(S), its exponent-two/one exits witness the B-selecting obligation O(S,1-g), and Section 25 transfers DRAW to the adjacent frame over the actual child B(S), with valuation at least 3 only at v=10 and exactly 2 thereafter | **PROVED SECOND-LEVEL B-OBLIGATION** | Section 106 of docs/verified-results.md; regression in tests/test_game.py |
| For every exponent-one factor-free lift S=Q_1^g(J(r)), the positive B-child k=B(S) has coefficient source rho(k)<r; in the second high-return obligation this makes k DRAW an immediate nested-source descent, and S is ordinary in every v>=11 row | **PROVED NESTED-SOURCE DESCENT** | Section 107 of docs/verified-results.md; regression in tests/test_game.py |
| For every v>=11 second-level high-return row, k=B(S) is exactly Q_(j-1)^(1-g)(J(z)) over the actual child z=B(r), and the valuation-two transfer gives O(k,g); v=11 is A-selecting, v>=12 is B-selecting, and z DRAW at v>=12 exposes a boundary WIN two proof levels below r | **PROVED ACTUAL-CHILD NESTED RE-ENTRY** | Section 108 of docs/verified-results.md; regression in tests/test_game.py |
| For every v>=12 stable nested re-entry, x=B(A(r)) is a WIN at least two proof levels below r, and the next B-transition source is an exact lift over x at v=12, A(x) at v=13, and B(x) at v>=14; the corresponding valuations are 2, 3, at least 5, and 4 | **PROVED SECOND NESTED LOWER-TOKEN ATTACHMENT** | Section 109 of docs/verified-results.md; regression in tests/test_game.py |
| For every ordinary constant-tail WIN token X=Q_d^g(C) with d>=4, the recurrent common token B(A(X)) is Q_(d-3)^g(9C), is WIN, and has height at most h(X)-2; therefore the stable nested high-return recycle can repeat only finitely and ends at tail exponent 1, 2, or 3 | **PROVED STABLE NESTED-RECYCLE RANK** | Section 110 of docs/verified-results.md; regression in tests/test_game.py |
| Every adjacent Q4/Q3 DRAW frame reaches a raw/signed exit in its first three factor levels; in the stable v>=20 nested row those exits are respectively a Q3/Q2 frame over the marked source y, an exponent-one lift over its raw member, or a frame over an exact lift of y's actual sibling, while the marked WIN child falls by two proof levels | **PROVED THREE-LEVEL MARKED EXIT HORIZON** | Section 111 of docs/verified-results.md; regression in tests/test_game.py |
| A nonempty result from `certified_finite_draw_kernel` is a genuine finite DRAW countercertificate | **PROVED FOR THE IMPLEMENTATION LOGIC** | `docs/verified-results.md`, Section 8; `scripts/find_finite_draw_kernel.py` |
| Every starting value is `WIN` or `LOSS`, with no `DRAW` | **OPEN** | main problem |
| All odd starts below a large bound have been solved by the current code | **UNVERIFIED UNTIL REPRODUCED** | run scripts locally and record exact output |
| A finite-state transducer plus rank can prove the theorem | **CONJECTURAL PROGRAM** | `docs/research-plan.md` |
| A numerical rank on the coarse two-step exceptional-return abstraction suffices | **DISPROVED** | Exact cycle in `docs/pitfalls.md`, Section 9; regression in `tests/test_game.py` |
